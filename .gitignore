# Local-only research notes (personal; the build plan itself is committed)
docs/research-passwerk.md

# Dependencies / build
node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Env
.env
.env.*
!.env.example

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
*.log

# Oracle outputs and Python venvs
tools/oracle/out/
.venv/
__pycache__/

apps/web/playwright-report/
apps/web/test-results/
apps/mcp-app/playwright-report/
apps/mcp-app/test-results/

# Demo output (packages/cli/scripts/demo.*)
out/

# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# Compose bind-mount directories (created by users and by the smoke test)
documents/
output/

# MCP App workbench copied in by `pnpm build:mcp-app` (ADR D-037); ships in the server tarball
packages/server/ui/

# Local screen recordings; the compressed demo lives on the release page
demo/

# Dev-only MCP App host page, rebuilt by `build:host` before every e2e run (CI does the same)
apps/mcp-app/dist-host/
