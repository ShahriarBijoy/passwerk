/**
 * @passwerk/rules — bundled, checksummed, offline artefacts and knowledge base.
 *
 * Phase 0 placeholder. Phase 1 adds the IDTA 02035 templates, AAS schemas,
 * the EC data-point matrix, the attribute knowledge base and the legal timeline.
 */
export const PACKAGE_NAME = '@passwerk/rules' as const;
