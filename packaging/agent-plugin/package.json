{
  "name": "@passwerk/agent-plugin",
  "version": "0.0.0",
  "private": true,
  "description": "Generates plugins/passwerk: the Claude Code and Codex plugin both hosts install straight from this GitHub repository",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.mjs"
  }
}
